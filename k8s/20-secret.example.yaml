# 占位 Secret。真正的值通过 kubectl create secret 命令外部注入，详见 README。
# kubectl -n foundry-monitor create secret generic mailer-webhook \
#   --from-literal=url='<POWER_AUTOMATE_WEBHOOK_URL>'
apiVersion: v1
kind: Secret
metadata:
  name: mailer-webhook
  namespace: foundry-monitor
type: Opaque
stringData:
  url: REPLACE_ME
